#include <fstream>
#include <iostream>
#include <vector>
#include <string>

using namespace std;

vector<vector<char>> map(150, vector<char>(147));

vector<char> s;

int main() {
    std::ifstream fin("./input/input13.txt");

    string line;
    getline(fin, line);
    vector<int> v;
    v.push_back(3);
    
    cout << line;
    for(int i = 0; i < 150; i++) {
        
    }

    
    return 0;
}